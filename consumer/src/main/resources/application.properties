#logging.level.org.springframework.integration=debug
#
bootiful.sql.initialization-schema=always
spring.integration.jdbc.initialize-schema=${bootiful.sql.initialization-schema}
spring.sql.init.mode=${bootiful.sql.initialization-schema}
#
spring.datasource.url=jdbc:postgresql://localhost/postgres
spring.datasource.username=postgres
spring.datasource.password=postgres
server.port=0
